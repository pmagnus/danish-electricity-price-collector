spring:
  application:
    name: danish-electricity-price-collector
    
  datasource:
    url: jdbc:postgresql://192.168.50.195:5432/electricity_prices
    username: postgres
    password: ratata,123
    driver-class-name: org.postgresql.Driver
    
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
    validate-on-migrate: true
        
  thymeleaf:
    cache: false
    
server:
  port: 8080
  
logging:
  level:
    dk.electricity: DEBUG
    org.springframework.web: DEBUG